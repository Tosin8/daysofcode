import 'package:flutter/material.dart';

class TravelDiscover extends StatelessWidget {
  const TravelDiscover({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Discover Page")));
  }
}
